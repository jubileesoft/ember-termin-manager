mutation CreateTenant($input: CreateTenantInput) {
  createTenant(input: $input) {
    id
    name
    short
    config {
      id
      admin {
        id
        email
      }
      agents {
        id
        email
      }
    }
  }
}